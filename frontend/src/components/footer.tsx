import { component$ } from "@builder.io/qwik";

export const Footer = component$(() => {
  return (
    <footer class=" body bottom-4 w-full text-center">
      &copy; 2024 ALBA RUS
    </footer>
  );
});
