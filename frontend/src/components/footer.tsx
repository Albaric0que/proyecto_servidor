import { component$ } from "@builder.io/qwik";

export const Footer = component$(() => {
  return (
    <footer class=" body text-books-800 bottom-4 w-full text-center font-bold">
      &copy; 2024 ALBA RUS
    </footer>
  );
});
